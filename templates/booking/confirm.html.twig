{% extends 'base.html.twig' %}

{% block title %}{{ 'title.reservation'|trans }}{% endblock %}

{% block body %}

	{{ include('_flash-message.html.twig') }}

	<div class="max-w-2xl mx-auto p-8 sm:p-7 md:p-6 lg:p-4">

		<div class="w-fit mx-auto text-center mb-8">
			<span class="material-symbols-rounded title-icon text-textColor">calendar_today</span>
			<h1 class="textH1">{% trans %}h1.reservation{% endtrans %}</h1>
		</div>

		<div class="card text-lightText" id="booking-card">

			<div class="text-lightText">
				<div class="text-xl mb-6">
					{{ 'reservation.title'|trans }}
				</div>
				<div class="mb-6">
					{{ 'reservation.text'|trans }} <br><span class="font-bold">{{ user.email }}</span>
				</div>
				<div class="text-end mb-6">
					{{ 'reservation.thanks'|trans }}
				</div>
				<div class="flex justify-center">
					<a class="btn" href="{{ path('app_home') }}">Retour à l'accueil</a>
				</div>
			</div>

		</div>

	</div>
{% endblock %}



