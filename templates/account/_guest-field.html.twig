<div class="border-2 border-secondary rounded mb-6 p-3">
	<p class="w-full text-white text-center border-b border-secondary p-2 mb-2">
		{% if index == 0 %}
		Vos informations
		{% else %}
		Convive n°{{ index }}
		{% endif %}
	</p>
	<p class="text-white">{{ firstname }}</p>
	<p class="text-white">{{ adult ? 'Adulte' : 'Enfant' }}</p>
	{% set all = concatAllergies(allergies) %}
	<p class="text-white">
		{{ all }}
	</p>
	<div class="flex justify-end">
		{% if allowDelete %}
			{{ include('account/_delete-guest.html.twig', {id: guestId}) }}
		{% endif %}
		<button hx-get="{{ editPath ?? path('app_account_edit_guest') }}" class="icon-btn">
			<span class="material-symbols-rounded">Edit</span>
		</button>
	</div>
</div>