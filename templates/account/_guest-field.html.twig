<div class="border border-2 border-black rounded mb-6 p-3">
	<p class="w-full text-center">Convives n°{{ index }}</p>
	<p>{{ firstname }}</p>
	<p>{{ adult ? 'Adulte' : 'Enfant' }}</p>
	{% set all = concatAllergies(allergies) %}
	<p>
		{{ all }}
	</p>
	<div class="flex justify-end">
		<button hx-get="{{ editPath ?? path('app_account_edit_guest') }}" class="icon-btn">
			<span class="material-symbols-rounded">Edit</span>
		</button>
	</div>
</div>