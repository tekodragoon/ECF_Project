<div
		id="{{ 'cat-%d'|format(id) }}"
		class="flex gap-4 items-center">

	<div class="flex-grow bg-white p-3 rounded">
		{{ name }}
	</div>

	{{ include('management/category/_manage-category.html.twig', {
		id: id,
		allowDelete: allowDelete
	}) }}

</div>